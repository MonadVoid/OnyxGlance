{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Onyx Glance",
            "type": "process",
            "command": "${workspaceFolder}/../../build/win32_og.exe",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}/../../build"
            },
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}
